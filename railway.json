{
    "services": {
      "web": {
        "buildCommand": "npm install --force && npm run build",
        "runCommand": "npm start",
        "env": {
          "NODE_ENV": "production"
        }
      }
    }
  }